// Package helpers provides shared test utilities for Pebble integration tests.
// fixtures will seed users, wallets, and sample transactions for repeatable integration cases.
package helpers

// TODO: implement fixtures — SeedDevUser(pool, email), SeedTransaction(pool, userID), etc.
